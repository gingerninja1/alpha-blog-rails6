<h1>Showing Article Details</h1>

<p><strong>Title:</strong> <%= @article.title %></p>
<p><strong>Description:</strong> <%= @article.description %></p>

<p><%= link_to 'Edit', edit_article_path(@article), {class: 'btn btn-primary'} %> <%= link_to 'Delete', article_path(@article), method: :delete, class: 'btn btn-primary', data: { confirm: 'Are you sure?' } %></p>
<p><%= link_to 'Return to article listings', articles_path %></p>
